<h1>Add a review</h1>
<h3>You are writing a review for <%= @restaurant.name %></h3>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>

  <%= f.submit 'Add review', class: 'btn btn-outline-primary'  %>
<% end %>
